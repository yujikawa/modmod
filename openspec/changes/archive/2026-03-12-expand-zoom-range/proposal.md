## Why
大規模なデータモデルを表示した際、現在のデフォルトズーム制限（0.5倍）では全体を俯瞰することが困難です。より広い範囲を一度に見れるようにズームアウトの限界値を下げます。

## What Changes
- `ReactFlow` コンポーネントに `minZoom={0.05}` を追加し、5%までズームアウト可能にします。
- ついでに `maxZoom={2}` を設定し、ズームインの範囲も調整します。
