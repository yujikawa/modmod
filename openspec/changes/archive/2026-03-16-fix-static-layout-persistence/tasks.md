## 1. バグ修正

- [x] 1.1 `visualizer/src/App.tsx` の `onNodeDragStop` から `if (!isCliMode) return;` を削除
- [x] 1.2 `visualizer/src/App.tsx` の `onSelectionDragStop` から `if (!isCliMode) return;` を削除

## 2. ビルド確認

- [x] 2.1 `npm run build-ui` でビルドが通ることを確認
