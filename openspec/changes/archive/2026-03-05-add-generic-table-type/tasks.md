## 1. Schema & Node Configuration

- [x] 1.1 Add `'table'` to `type` Union in `visualizer/src/types/schema.ts`
- [x] 1.2 Add `table` config (Slate color, Clipboard icon) to `TYPE_CONFIG` in `visualizer/src/components/TableNode.tsx`

## 2. UI Integration

- [x] 2.1 Add "Table" option to the type dropdown in `visualizer/src/components/DetailPanel.tsx`

## 3. Template Documentation

- [x] 3.1 Update `src/templates/rules.md` to include `table` in the modeling strategy and schema reference

## 4. Verification

- [x] 4.1 Verify a node can be set to type `table` and displays correctly
- [x] 4.2 Verify the change persists to YAML
- [x] 4.3 Verify the Slate color looks good in both Dark and Light modes
