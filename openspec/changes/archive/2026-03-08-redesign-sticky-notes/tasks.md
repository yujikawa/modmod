## 1. コンポーネントロジックの更新

- [x] 1.1 `AnnotationNode.tsx` で ID に基づいたランダムな回転角（`-1.5` 〜 `1.5` 度）を生成するロジックを実装する
- [x] 1.2 `isCallout` や `annotation.type` による条件分岐を整理し、デザインを単一の「付箋」形式に集約する準備をする

## 2. デザインとレイアウトの実装

- [x] 2.1 右下のめくれ（Dog-ear）効果を `linear-gradient` と `div` で実装する
- [x] 2.2 テキストコンテナに `max-height: 240px` と `overflow-y: auto` を適用する
- [x] 2.3 カスタムスクロールバーのスタイルを追加し、テーマに馴染ませる

## 3. ゴミ箱UIの配色改善

- [x] 3.1 ライトモード時のゴミ箱ツールバーの背景色を `white` に変更し、枠線を調整する
- [x] 3.2 テーマ（Dark/Light）に応じたゴミ箱アイコンとホバー時の配色を設計通りに実装する

## 4. 仕上げと検証

- [x] 4.1 長文を入力し、正常に内部スクロールされることを確認する
- [x] 4.2 コネクタ（ターゲットとの線）が表示されている場合でも、デザインが崩れないことを確認する
- [x] 4.3 ダークモードとライトモードの両方で、ゴミ箱ボタンの視認性が向上していることを確認する
- [x] 4.4 `npm run build` を実行してビルドエラーがないか確認する
