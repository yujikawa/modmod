## 1. キーボードイベント基盤の強化

- [x] 1.1 `App.tsx` の `handleKeyDown` 内で、テキスト入力中であることを判定する `isTyping` ロジックを最新 spinning UI 構成（検索窓、詳細パネル等）に合わせて再検証・強化する
- [x] 1.2 `e.repeat` (キーの長押し) を検知して、オブジェクトが連続生成されないように制限を加える

## 2. オブジェクト作成ショートカットの実装

- [x] 2.1 `T` キーが押された際、画面中央にテーブルを追加するロジックを実装する
- [x] 2.2 `D` キーが押された際、画面中央にドメインを追加するロジックを実装する
- [x] 2.3 `S` キーが押された際、画面中央に付箋を追加するロジックを実装する

## 3. 検証と仕上げ

- [x] 3.1 YAMLエディタ、エンティティ検索窓、付箋のテキスト編集、詳細パネルの各入力欄でショートカットが発火しないことを確認する
- [x] 3.2 キャンバス上の何もない場所をクリックした後、T, D, S キーで期待通りにオブジェクトが生成されることを確認する
- [x] 3.3 ズームやスクロールの状態に関わらず、常に「今見えている画面の中央」に配置されることを確認する
- [x] 3.4 `npm run build` を実行してビルドエラーがないか確認する
