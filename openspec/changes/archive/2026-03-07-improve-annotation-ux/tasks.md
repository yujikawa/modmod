## 1. コンポーネントの更新 (AnnotationNode)

- [x] 1.1 `AnnotationNode.tsx` に `isNew` ステート（初期値: true）を追加する
- [x] 1.2 `useEffect` を使用して、マウントから1秒後に `isNew` を false にする
- [x] 1.3 ノードの外側の `div` に `isNew ? 'animate-creation' : ''` クラスを適用する
- [x] 1.4 ドラッグ可能な状態の時に `cursor: 'grab'` を適用し、操作感を向上させる

## 2. ストアとアクションの更新 (useStore)

- [x] 2.1 `addAnnotation` アクション内で、ターゲットがない場合にカメラフォーカスを設定するように修正する (`setFocusNodeId`)
- [x] 2.2 `addAnnotation` 実行時に詳細パネルが自動で開かないように制御する (既存の `selectedAnnotationId` 設定時のサイドエフェクトを確認)

## 3. ツールバーと座標計算の更新 (CanvasToolbar)

- [x] 3.1 `CanvasToolbar.tsx` の付箋追加ボタン (`Tag` アイコン) のハンドラを修正する
- [x] 3.2 ターゲットなしで追加する場合の座標が画面中央に正しく配置されるよう調整する

## 4. 動作検証

- [x] 4.1 ターゲット（テーブル）を選択した状態で付箋を追加し、アニメーションのみが再生され、詳細パネルが開かないことを確認する
- [x] 4.2 何も選択せずに付箋を追加し、画面中央にアニメーション付きで出現し、カメラが自動フォーカスされることを確認する
- [x] 4.3 付箋のテキスト編集（ダブルクリック）が正常に機能し、保存されることを確認する
