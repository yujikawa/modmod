## 1. Implement Toggle Logic in App.tsx

- [x] 1.1 Update `onNodeClick` in `visualizer/src/App.tsx` to toggle node selection (deselect if already selected)
- [x] 1.2 Implement `onEdgeClick` in `visualizer/src/App.tsx` to toggle edge selection (deselect if already selected)
- [x] 1.3 Update the `ReactFlow` component in `visualizer/src/App.tsx` to use the new `onEdgeClick` prop

## 2. Verification

- [x] 2.1 Verify that clicking a selected table/domain deselects it
- [x] 2.2 Verify that clicking a selected edge deselects it
- [x] 2.3 Verify that clicking a different node/edge updates the selection correctly
