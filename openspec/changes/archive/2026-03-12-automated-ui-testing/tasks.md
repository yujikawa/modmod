## 1. 環境構築

- [x] 1.1 `playwright` および `@playwright/test` を `devDependencies` に追加する
- [x] 1.2 `npx playwright install chromium` でテスト用ブラウザをインストールする
- [x] 1.3 `playwright.config.ts` を作成し、ベースURLやテストディレクトリを設定する

## 2. テスト基盤の実装

- [x] 2.1 `tests/fixtures/test-model.yaml` を作成し、テスト用の標準モデルを定義する
- [x] 2.2 テスト実行前に CLI サーバーを自動起動するグローバルセットアップ（または `webServer` 設定）を構成する
- [x] 2.3 `tests/utils/` に WebSocket 待機やノード取得のヘルパー関数を作成する

## 3. 基本テストケースの実装

- [x] 3.1 **初期ロードテスト**: YAML の内容が正しくノードとして描画されていることを検証する
- [x] 3.2 **詳細パネルテスト**: ノードクリックで詳細情報（カラム名など）が表示されることを検証する
- [x] 3.3 **検索機能テスト**: サイドバーでの検索が動作し、リストがフィルタリングされることを検証する
- [x] 3.4 **リアルタイム同期テスト**: テストコードからファイルを書き換え、ブラウザが自動更新されることを検証する

## 4. ビジュアルテストとCI連携

- [x] 4.1 主要な画面状態（サイドバー展開時、詳細パネル表示時）のスクリーンショット比較テストを追加する
- [x] 4.2 `package.json` に `test:e2e` スクリプトを追加する
- [x] 4.3 (Optional) GitHub Actions で PR 時ごとに E2E テストが走るようワークフローを更新する
